resources:
  'entity:node':
    GET:
      supported_formats:
        - hal_json
      supported_auth:
        - basic_auth
    POST:
      supported_formats:
        - hal_json
      supported_auth:
        - basic_auth
    PATCH:
      supported_formats:
        - hal_json
      supported_auth:
        - basic_auth
    DELETE:
      supported_formats:
        - hal_json
      supported_auth:
        - basic_auth
  georeport_request_index_resource:
    GET:
      supported_formats:
        - xml
        - json
      supported_auth:
        - cookie
    POST:
      supported_formats:
        - xml
        - form
        - json
      supported_auth:
        - api_key_auth
  georeport_request_resource:
    GET:
      supported_formats:
        - xml
        - json
      supported_auth:
        - cookie
  georeport_service_index_resource:
    GET:
      supported_formats:
        - xml
        - json
      supported_auth:
        - cookie
link_domain: null
